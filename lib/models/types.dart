import 'package:flutter/material.dart';

class Type {
  final String id;
  final String title;
  final Color color;

  const Type(
      {required this.id, required this.title, this.color = Colors.green});
}
